apiVersion: v1
kind: Service
metadata:
  name: url-shortener-service
  labels:
    app: url-shortener-backend
spec:
  type: LoadBalancer
  selector:
    app: url-shortener-backend
  ports:
    - port: 80
      targetPort: 3000
      name: http
